# build_release.sh

This script is used to build release packages for Linux, Mac and Windows for amd64 and arm64. 

## Usage

```bash
./build_release.sh <version> <changelog_path> [build for EIS (true|false, default false)]
```
