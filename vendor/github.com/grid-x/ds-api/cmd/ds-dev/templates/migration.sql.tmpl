-- +migrate Up
CREATE TABLE {{ underscore .Name }} (
{{- $c := counter -}}
{{- range $index, $attribute := .Attributes -}}{{- if call $c -}},{{- end }}
{{ dbColumn .Name }} {{ dbType .Type -}}
{{ end }}
);

-- +migrate Down
DROP TABLE {{ underscore .Name }};
